export default defineEventHandler(event => {
  // const token = getHeader(event, 'authorization')
  // if (!token) {
  //   throw createError({ statusCode: 401, statusMessage: 'Falta token' })
  // }
  console.log('auth')
})
